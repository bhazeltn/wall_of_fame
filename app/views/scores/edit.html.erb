</div>
</div>

<div style="text-align: center;">
  <h3>Update Score</h3>
  <h4><%= @score.event.level.name %> <%= @score.event.name %></h4>
  <% if @score.segment.name != "Total" %>
    <h5><%= @score.segment.name %></h5>
    <% if @score.score_type.score_type != "Total" %>
      <h5><%= @score.score_type.score_type %></h5>
    <% end %>
  <% end %>
</div>
<div class="container">

<%= render 'form', score: @score %>
</div>

<%= link_to 'Show', @score %> |
<%= link_to 'Back', scores_path %>
