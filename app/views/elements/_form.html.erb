<%= form_with(model: element, local: true) do |form| %>
  <% if element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(element.errors.count, "error") %> prohibited this element from being saved:</h2>

      <ul>
      <% element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :element_type_id %>
    <%= form.text_field :element_type_id, id: :element_element_type_id %>
  </div>

  <div class="field">
    <%= form.label :abbrev %>
    <%= form.text_field :abbrev, id: :element_abbrev %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :element_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
